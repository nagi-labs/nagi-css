# Nagi CSS Vue minimal example

This isolated workspace installs the published Nagi CSS 0.4.0 ESLint plugin;
it does not resolve packages from the parent repository.

Requirements: Node.js 22.18 or newer and `vp`.

```sh
vp install --frozen-lockfile
vp run lint
vp run build
vp run dev
```
